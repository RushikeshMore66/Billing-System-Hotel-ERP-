import { AppShell } from "../../shell/AppShell";
export default function AIInsightsLayout({ children }: { children: React.ReactNode }) {
  return <AppShell pageTitle="AI Insights">{children}</AppShell>;
}
