[tool.poetry]
name = "niralay-os"
version = "0.1.0"
description = "Enterprise Resort & Restaurant Management Platform"
authors = []

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
